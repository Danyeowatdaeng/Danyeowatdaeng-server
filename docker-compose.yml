services:
  redis:
    image: redis:7.0
    networks:
      - tourapi-network

  app:
    image: ${DOCKERHUB_USERNAME}/tourapi:latest
    env_file:
      - .env
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    ports:
      - '8080:8080'  # API는 외부에 열어야 하니까 이건 OK
    depends_on:
      - redis
    networks:
      - tourapi-network

networks:
  tourapi-network:
    driver: bridge
